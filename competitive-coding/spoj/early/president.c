#include<stdio.h>
int main(int agrc,char **agrv)
{
    int tc,i,v,c;
    scanf("%d",&tc);
    while(tc--)
    {
        scanf("%d %d",&c,&v)
        for(i=0;i<v;i++)
        {
           scanf("%d %d",rnd1[],rnd2[]);

        }
    }
}
